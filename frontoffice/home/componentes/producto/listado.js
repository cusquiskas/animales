var listado = class {
    
    listado (s, d, e) {
        console.log('listado.js -> listado');
    }
    
    constructor (a, b, c) {
        console.log('listado.js -> constructor');
        //Moduls.getVentana().getForm('listaArticulos').executeForm();
    }
}